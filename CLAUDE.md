# Claude Code Entry

Shared project rules and the authoritative evidence boundary live in `AGENTS.md`.

@import AGENTS.md

If Claude Code leaves `@import` as literal text, read `AGENTS.md` and the status
document it names before editing.
